{% extends 'base.html.twig' %}

{% block title %}Investissement index{% endblock %}

    
{% block body %}

       <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.22/css/jquery.dataTables.min.css">
       <script src="https://code.jquery.com/jquery-3.6.0.js"></script>
       <script type="text/javascript" src="https://cdn.datatables.net/1.10.22/js/jquery.dataTables.min.js"></script>

       <script type="text/javascript">
       $(document).ready( function () {
        $('#myTable').DataTable();
    } );
    </script>
    <div class="container">
    <h1 class="text-center mb-4">Investissement list</h1>

    <div class="row mb-3">
    <div class="col">
    <a href="{{ path('app_investissement_new') }}" class="btn btn-primary">Create New</a>
    </div>
    {# <div class="col">
    <form action="{{ path('app_investissement_index') }}" method="get" class="form-inline">
    <div class="input-group">
    <input type="text" name="search" class="form-control" placeholder="Search...">
    <div class="input-group-append">
    <button type="submit" class="btn btn-outline-secondary">Search</button>
    </div>
    </div>
    </form>
    </div> #}
    </div>

    <div class="table-responsive">
    <table class="table table-striped table-hover" id="myTable">
    <thead class="thead-dark">
    <tr>
    <th scope="col">Id</th>
    <th scope="col">Nom</th>
    <th scope="col">Montant Initial</th>
    <th scope="col">Montant Retour</th>
    <th scope="col">Description</th>
    <th scope="col">Date</th>
    <th scope="col">Actions</th>
    </tr>
    </thead>
    <tbody>
    {% for investissement in investissements %}
        <tr>
        <td>{{ investissement.id }}</td>
        <td>{{ investissement.nom }}</td>
        <td>{{ investissement.montantInitial }}</td>
        <td>{{ investissement.montantRetour }}</td>
        <td>{{ investissement.description }}</td>
        <td>{{ investissement.date ? investissement.date|date('Y-m-d') : '' }}</td>
        <td>
        <a href="{{ path('app_investissement_show', {'id': investissement.id}) }}" class="btn btn-info btn-sm">Show</a>
        <a href="{{ path('app_investissement_edit', {'id': investissement.id}) }}" class="btn btn-primary btn-sm">Edit</a>
        
        <div class="row mb-3">


        </div>
        </form>
        </div>
        </div>




        </td>
        </tr>

        {% else %}
            <tr>
            <td colspan="7">No records found</td>
            </tr>
        {% endfor %}
        </tbody>
        </table>
        </div>
        </div>
    {% endblock %}
